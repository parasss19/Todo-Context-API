//This file is used just to import everything in context folder 
//and where ever we need things from context folder we just use this index file

//we directly take "useTodo" so we dont have to import each method separately
export {TodoContext, TodoProvider, useTodo} from './TodoContext'

